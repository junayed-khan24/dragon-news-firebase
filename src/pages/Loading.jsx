import React from 'react';

const Loading = () => {
    return (
        <div className="min-h-screen flex justify-center items-center ">
            <span className="loading loading-ring text-error  loading-xl"></span>
        </div>
    );
};

export default Loading;